import streamlit as st
import pandas as pd
st.set_page_config(layout='wide')
st.title('Traffic Violations Dashboard (Stub)')
st.info('This is a lightweight Streamlit dashboard stub. Connect to parquet outputs to visualize.')
st.write('Add charts, filters and maps here.')
